---
description: Compare a metric between two time periods using Ana
argument-hint: <metric> <period A> vs <period B>
---

Use the `ana` MCP tool to compare a metric between two time periods.

Request: $ARGUMENTS

Ask Ana for the metric in each period, then report both values, the absolute
change, and the percentage change. Note any caveats (partial periods, missing
data) Ana surfaces, and include the Ana chat link.
